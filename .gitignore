node_modules/
dist/
*.log
.env
gate-chain/.env
execution/.env

# Real .mcp.json holds a live RPC key + KEYSTORE_MASTER_KEY -- never commit
# it. Copy .mcp.example.json to .mcp.json and fill in your own values.
.mcp.json
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.thetanuts/

# Duplicate of docs/Thetanuts MUBA Hackathon.pdf -- the original save location,
# kept as-is because the sandbox couldn't unlink it after copying into docs/.
/Thetanuts MUBA Hackathon.pdf
